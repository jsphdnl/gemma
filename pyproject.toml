[project]
name = "turboquant-kv-cache"
version = "0.1.0"
description = "TurboQuant-style KV cache compression prototype"
requires-python = ">=3.14"
dependencies = [
    "accelerate>=1.13.0",
    "scipy>=1.17.1",
    "torch>=2.11",
    "transformers>=5.5.0",
]
